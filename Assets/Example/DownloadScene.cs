using UnityEngine;
using UnityEngine.UI;

namespace Example {
    public class DownloadScene : MonoBehaviour {
        private void Start () {
            GetComponent<Text>().text = "This is the download scene.";
        }
    }
}
